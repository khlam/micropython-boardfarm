[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ld2450"
version = "0.1.1"
description = "MicroPython driver for the Hi-Link HLK-LD2450 multi-target radar."
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["ld2450"]
