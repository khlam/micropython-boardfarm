[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ld2420"
version = "0.1.0"
description = "MicroPython driver for the Hi-Link HLK-LD2420 presence radar."
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["ld2420"]
