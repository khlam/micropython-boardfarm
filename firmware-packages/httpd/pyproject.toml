[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "httpd"
version = "0.1.0"
description = "Minimal asyncio HTTP and WebSocket server for MicroPython firmware."
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["httpd"]
