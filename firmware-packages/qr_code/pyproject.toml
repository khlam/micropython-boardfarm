[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "qr_code"
version = "0.1.0"
description = "Fixed Version 2 / level-L byte-mode QR code encoder for MicroPython."
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["qr_code"]
