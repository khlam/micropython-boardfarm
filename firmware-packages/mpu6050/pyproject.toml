[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mpu6050"
version = "0.2.0"
description = "MicroPython driver for the InvenSense MPU6050 / MPU6500 / MPU9250 IMU family."
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["mpu6050"]
