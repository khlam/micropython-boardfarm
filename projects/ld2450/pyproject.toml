[project]
name = "ld2450-demo"
version = "0.1.0"
description = "Display HLK-LD2450 radar targets on a live web dashboard."
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "boot_status_led>=0.2.0",
    "ld2450>=0.1.0",
]

[tool.uv]
package = false

[tool.uv.sources]
boot_status_led = { index = "internal" }
ld2450 = { index = "internal" }
