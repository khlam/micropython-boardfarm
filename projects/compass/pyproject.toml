[project]
name = "compass"
version = "0.1.0"
description = "QMC5883P magnetometer → JSON-over-USB-CDC → FastAPI/WebSocket → Plotly compass dashboard."
readme = "README.md"
requires-python = ">=3.12"


dependencies = [
    "micropython-boardfarm>=0.1.0",
]
