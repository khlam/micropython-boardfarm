[project]
name = "gps-stream"
version = "0.2.0"
description = "ATGM336H NMEA → 10-second JSON windows → FastAPI/WebSocket → dashboard."
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "micropython-boardfarm>=0.6.0",
]

[tool.uv]
package = false

[tool.uv.sources]
micropython-boardfarm = { workspace = true }
