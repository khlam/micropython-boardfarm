[project]
name = "matter-radar-sensor"
version = "0.5.5"
description = "MicroPython Matter occupancy-sensor firmware for an HLK-LD2450 or HLK-LD2420 radar."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "micropython-boardfarm>=0.20.0",
    "microdot==2.6.2",
]

[tool.uv]
package = false

[tool.uv.sources]
micropython-boardfarm = { workspace = true }
