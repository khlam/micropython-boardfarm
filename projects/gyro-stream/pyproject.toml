[project]
name = "gyro-stream"
version = "0.3.0"
description = "MPU6050 accel/gyro/temp → JSON-over-USB-CDC → Web Serial → Plotly + 3D orientation dashboard."
readme = "README.md"
requires-python = ">=3.12"


dependencies = [
    "micropython-boardfarm>=0.1.0",
]
