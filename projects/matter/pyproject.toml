[project]
name = "matter-example"
version = "0.1.0"
description = "MicroPython-owned Matter application over a native ESP-Matter backend."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "micropython-boardfarm>=0.10.0",
]

[tool.uv]
package = false

[tool.uv.sources]
micropython-boardfarm = { workspace = true }
