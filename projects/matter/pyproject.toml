[project]
name = "matter-example"
version = "0.2.3"
description = "MicroPython-owned Matter application over a native ESP-Matter backend."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "micropython-boardfarm>=0.16.0",
]

[tool.uv]
package = false

[tool.uv.sources]
micropython-boardfarm = { workspace = true }
