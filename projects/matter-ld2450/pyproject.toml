[project]
name = "matter-ld2450"
version = "0.1.0"
description = "MicroPython Matter presence-switch firmware for an HLK-LD2450 radar."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false
